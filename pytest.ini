[pytest]
pythonpath = source
testpaths = tests
filterwarnings =
    ignore:builtin type .* has no __module__ attribute:DeprecationWarning
    ignore:Failed to disconnect.*from signal "timeout\(\)":RuntimeWarning
